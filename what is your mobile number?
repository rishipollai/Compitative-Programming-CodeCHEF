#include<bits/stdc++.h>
using namespace std;
int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int n,f=0;
	cin>>n;
	while(n--)
	{
		string s;
		cin>>s;
	   long long int m=s.size(),i;
		if(m!=10 || s[0]=='0')
		{
			cout<<"NO"<<"\n";
		}
      else
	  {
		  for(i=0;i<m;i++)
		   {
			   int k=int(s[i]);  
			   if(k>=48 && k<=57)
			      f=0;			       
			   else
			     {f=1;break;}
			     
		   }
		   if(f==1)
		      cout<<"NO"<<"\n";
		   else
		       cout<<"YES"<<"\n";
	  }	    

	}
}
